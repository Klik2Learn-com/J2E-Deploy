{
  "name": "bundle",
  "version": "1.0.0",
  "description": "This is a Meteor application bundle. It has only one external dependency: Node.js v8.10.0. To run the application:",
  "main": "main.js",
  "engines": {
    "node": "8.10.0",
    "npm": "2.15.11"
  },
  "scripts": {
    "heroku-prebuild": "cd bundle/programs/server && npm install -q",
    "start": "cd bundle && node main.js"
  },
  "dependencies": {
    "babel-runtime": "^6.26.0",
    "bcrypt": "^5.0.0"
  }
}
